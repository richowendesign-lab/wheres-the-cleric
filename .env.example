DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Required in production — used to build campaign join URLs.
# Must NOT have a trailing slash. Without this, join URLs fall back to localhost.
NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"
