---
phase: 08-dm-auth
plan: 04
type: execute
wave: 4
depends_on:
  - "08-01"
  - "08-02"
  - "08-03"
files_modified:
  - src/app/campaigns/[id]/page.tsx
autonomous: false
requirements:
  - AUTH-04

must_haves:
  truths:
    - "DM can log out from a campaign page using a logout button/link"
    - "After logout, DM is redirected to /auth/login"
    - "Returning to a campaign page after logout redirects to /auth/login"
    - "DM session is deleted from the database on logout (not just cookie cleared)"
  artifacts:
    - path: "src/app/campaigns/[id]/page.tsx"
      provides: "Campaign dashboard with logout button that calls logOut server action"
      contains: "logOut"
  key_links:
    - from: "src/app/campaigns/[id]/page.tsx"
      to: "src/lib/actions/auth.ts"
      via: "logout button form calls logOut"
      pattern: "logOut"
---

<objective>
Add a logout button to the campaign dashboard page, then verify the complete end-to-end auth flow works in the browser.

Purpose: AUTH-04 requires DMs to be able to log out. The campaign dashboard is the primary authenticated page in Phase 8, so it's the natural place for the logout control. The checkpoint that follows confirms all four auth requirements work together.

Output: Campaign dashboard has a logout control; human verification confirms sign-up, login, session persistence, and logout all work correctly.
</objective>

<execution_context>
@/Users/richardowen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardowen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-dm-auth/08-01-SUMMARY.md
@.planning/phases/08-dm-auth/08-02-SUMMARY.md
@.planning/phases/08-dm-auth/08-03-SUMMARY.md
@src/app/campaigns/[id]/page.tsx
</context>

<interfaces>
<!-- Key contracts from Plans 01-03 that this plan's executor must use. -->

From src/lib/actions/auth.ts:
```typescript
export async function logOut(): Promise<never>
// Deletes session from DB, clears dm_session_token cookie, redirects to /auth/login
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add logout button to campaign dashboard</name>
  <files>src/app/campaigns/[id]/page.tsx</files>
  <action>
Read the current src/app/campaigns/[id]/page.tsx to understand its layout. Add a logout form button in the top-right area of the dashboard (or wherever the existing UI has space — a header area or a subtle placement at the top of the page).

Add the logout form at the top of the campaign dashboard content area:

```tsx
import { logOut } from '@/lib/actions/auth'

// In the JSX, add near the top of the page content (inside the main element, before the campaign content):
<form action={logOut} className="flex justify-end mb-4">
  <button
    type="submit"
    className="text-sm text-gray-400 hover:text-gray-200 transition-colors underline"
  >
    Log out
  </button>
</form>
```

This is a plain form with a server action — no 'use client' needed. The button submits the form which calls logOut, which deletes the session and redirects to /auth/login.

Also: The campaign dashboard currently checks for the `dm_secret` cookie to determine DM identity. Phase 8 does NOT change this ownership logic — that migration happens in Phase 9. For now, the existing dm_secret check can remain. Just add the logout button.

Do NOT break any existing campaign dashboard functionality. Add the logout button and import only — no other changes.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - src/app/campaigns/[id]/page.tsx imports logOut from '@/lib/actions/auth'
    - A form with action={logOut} and a submit button labeled "Log out" exists in the JSX
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: End-to-end auth flow verification</name>
  <action>Run `npm run dev` and manually verify all five auth flows described in how-to-verify below.</action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint. Run `npm run dev` and follow the verification flows below.</automated>
  </verify>
  <done>All five flows pass: sign-up, session persistence, logout, login, and sign-up validation errors.</done>
  <what-built>
    Complete DM auth system:
    - Sign-up page at /auth/signup (email + password, creates DM account, sets session cookie)
    - Login page at /auth/login (email + password, validates credentials, sets session cookie)
    - Session persists across browser refresh (httpOnly dm_session_token cookie, 30-day expiry)
    - Logout button on campaign dashboard (clears session, redirects to /auth/login)
    - Middleware protecting /campaigns/* (redirects to /auth/login if no cookie)
    - Home page shows Log In / Sign Up for unauthenticated visitors, redirects authenticated DMs to /campaigns
  </what-built>
  <how-to-verify>
Run `npm run dev` and test these flows in order:

**Flow 1 — Sign Up (AUTH-01):**
1. Visit http://localhost:3000 — confirm you see "Log In" and "Sign Up" buttons (not the old "Create a Campaign" button)
2. Click "Sign Up" — confirm you land at /auth/signup with email and password fields
3. Submit the form with a test email (e.g. test@example.com) and a password (e.g. password123)
4. Confirm you are redirected to / (home), which should then redirect to /campaigns (even if no campaigns exist yet — that's fine, Phase 9 builds the campaign list)
5. Confirm the dm_session_token cookie is set (DevTools > Application > Cookies — it should be httpOnly)

**Flow 2 — Session Persistence (AUTH-03):**
6. After signing up (still logged in), close the browser tab and reopen http://localhost:3000
7. Confirm you are redirected to /campaigns (NOT shown the Log In / Sign Up buttons) — session persisted

**Flow 3 — Logout (AUTH-04):**
8. Navigate to a campaign page (if you have one from previous testing) or go to /campaigns
9. Find the "Log out" link on the campaign dashboard
10. Click it — confirm you are redirected to /auth/login
11. Try navigating to /campaigns directly — confirm you are redirected to /auth/login (not reaching the page)

**Flow 4 — Log In (AUTH-02):**
12. Visit /auth/login with the email and password from step 3
13. Submit — confirm you are redirected to / (then to /campaigns)
14. Test wrong password — confirm "Invalid email or password." error appears inline (not a full page error)
15. Test wrong email — confirm same "Invalid email or password." error (no information leakage about which field is wrong)

**Flow 5 — Sign Up validation:**
16. Visit /auth/signup and submit with password shorter than 8 chars — confirm "Password must be at least 8 characters." error
17. Try signing up with the same email as step 3 — confirm "An account with this email already exists." error
  </how-to-verify>
  <resume-signal>Type "approved" if all flows work correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All four auth requirements verified by human:
- AUTH-01: DM can sign up with email and password
- AUTH-02: DM can log in with email and password
- AUTH-03: Session persists across browser refresh
- AUTH-04: DM can log out; /campaigns/* redirects to login when unauthenticated
</verification>

<success_criteria>
All five verification flows complete without errors. DM auth infrastructure is live and functional.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dm-auth/08-04-SUMMARY.md`
</output>
