---
phase: 10-multi-campaign-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/campaigns/page.tsx
autonomous: false
requirements:
  - CAMP-04
  - CAMP-05

must_haves:
  truths:
    - "Authenticated DM sees a card for each of their campaigns, showing the campaign name"
    - "Clicking a campaign card navigates to that campaign's dashboard (/campaigns/[id])"
    - "A 'Create new campaign' button is present and navigates to /campaigns/new"
    - "A DM with no campaigns sees an empty state with only the 'Create new campaign' button"
    - "Unauthenticated visitors are redirected away (handled by existing middleware)"
  artifacts:
    - path: "src/app/campaigns/page.tsx"
      provides: "DM home dashboard — campaign cards, empty state, create button"
      min_lines: 40
  key_links:
    - from: "src/app/campaigns/page.tsx"
      to: "getSessionDM()"
      via: "import from @/lib/auth"
      pattern: "getSessionDM"
    - from: "src/app/campaigns/page.tsx"
      to: "prisma.campaign.findMany"
      via: "import from @/lib/prisma"
      pattern: "findMany.*dmId"
    - from: "CampaignCard link"
      to: "/campaigns/[id]"
      via: "Next.js Link href"
      pattern: "href.*campaigns.*id"
---

<objective>
Replace the placeholder /campaigns page with the DM multi-campaign home dashboard.

Purpose: DMs accumulate multiple campaigns. The current /campaigns page blindly redirects to /campaigns/new — it needs to become the home that shows all their campaigns as clickable cards.

Output: src/app/campaigns/page.tsx — a Server Component that fetches the DM's campaigns and renders cards (name, link to dashboard) plus a "Create new campaign" button. Empty state when no campaigns exist.
</objective>

<execution_context>
@/Users/richardowen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardowen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From src/lib/auth.ts:
```typescript
export async function getSessionDM(): Promise<DM | null>
// Returns the authenticated DM from the session cookie, or null if not logged in.
// DM shape: { id: string, email: string, passwordHash: string, createdAt: Date }
```

From prisma/schema.prisma — Campaign model fields available for display:
```
id          String   (cuid)
name        String?  (required at creation, nullable in DB)
description String?
maxPlayers  Int?
dmId        String?
joinToken   String
createdAt   DateTime
```

From src/lib/actions/auth.ts:
```typescript
export async function logOut(): Promise<void>
// Server action — clears session cookie and redirects to /
```

Existing style patterns (dark theme, amber accents):
- Background: bg-gray-950
- Text: text-gray-100, text-gray-400
- Accent: text-amber-400, bg-amber-500
- Cards: bg-gray-900 border border-gray-800 rounded-lg
- Fantasy heading font: font-fantasy
- Button (primary): px-6 py-3 rounded bg-amber-500 text-gray-950 font-semibold hover:bg-amber-400 transition-colors
- Button (outline): px-6 py-3 rounded border border-amber-500 text-amber-400 font-semibold hover:bg-amber-500/10 transition-colors
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace /campaigns placeholder with DM home dashboard</name>
  <files>src/app/campaigns/page.tsx</files>
  <action>
Replace the entire contents of src/app/campaigns/page.tsx. The current file is a placeholder that redirects all visitors to /campaigns/new — Phase 10 replaces it with the multi-campaign dashboard.

Implementation:

1. Mark as async Server Component (no 'use client').

2. Fetch the authenticated DM using getSessionDM(). The middleware already protects /campaigns so an authenticated DM is guaranteed — but if getSessionDM() returns null (edge case), redirect to '/auth/login'.

3. Query the DM's campaigns:
```typescript
const campaigns = await prisma.campaign.findMany({
  where: { dmId: dm.id },
  orderBy: { createdAt: 'desc' },
  select: { id: true, name: true, createdAt: true },
})
```

4. Render the page layout (consistent with the rest of the app):
- Page wrapper: `<main className="min-h-screen bg-gray-950 text-gray-100 px-4 py-12">`
- Inner container: `<div className="max-w-2xl mx-auto">`
- Header row: fantasy heading "Your Campaigns" (text-amber-400 font-fantasy text-3xl) on the left, logout form on the right (plain HTML form with action={logOut}, same style as campaign dashboard — text-sm text-gray-400 hover:text-gray-200 underline)
- Below header: "Create new campaign" button as a Next.js Link to /campaigns/new — use the primary amber button style (bg-amber-500)

5. Campaign list — two branches:

EMPTY STATE (campaigns.length === 0):
- A paragraph below the create button: "No campaigns yet." in text-gray-500, with a subtle nudge ("Click the button above to create your first one.").
- Keep it minimal — do not add illustration or complex UI.

NON-EMPTY STATE (campaigns.length > 0):
- Render a vertical list of campaign cards (flex flex-col gap-4, margin-top below the create button).
- Each card is a Next.js Link wrapping a div:
  ```
  href={`/campaigns/${campaign.id}`}
  className="block bg-gray-900 border border-gray-800 rounded-lg px-6 py-4 hover:border-amber-700 hover:bg-gray-800 transition-colors"
  ```
- Inside each card:
  - Campaign name: `<h2 className="text-lg font-semibold text-gray-100">{campaign.name ?? 'Untitled Campaign'}</h2>`
  - A subtle "View dashboard →" line: `<p className="text-sm text-gray-500 mt-1">View dashboard →</p>`

The logout form should use `import { logOut } from '@/lib/actions/auth'` and render as a plain HTML form (no 'use client' needed — same pattern used on the campaign dashboard page).
  </action>
  <verify>
    <automated>cd /Users/richardowen/Desktop/my-portfolio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - src/app/campaigns/page.tsx is a Server Component that fetches campaigns via prisma
    - No redirect to /campaigns/new exists in the file
    - TypeScript compiles without errors
    - Campaign cards link to /campaigns/[id]
    - "Create new campaign" links to /campaigns/new
    - Logout form is present
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify DM home dashboard in browser</name>
  <what-built>
    DM home dashboard at /campaigns showing campaign cards, "Create new campaign" button, and empty state when no campaigns exist.
  </what-built>
  <how-to-verify>
    Run the dev server (npm run dev) if not already running.

    Test 1 — DM with campaigns:
    1. Log in as a DM who has at least one campaign (create one first via /campaigns/new if needed)
    2. Navigate to http://localhost:3000/campaigns
    3. Confirm: campaign cards appear with the campaign name
    4. Confirm: clicking a card navigates to the campaign dashboard (/campaigns/[id])
    5. Confirm: "Create new campaign" button is visible and links to /campaigns/new
    6. Confirm: Logout button is visible in the top-right area

    Test 2 — DM with no campaigns:
    1. Create a fresh DM account (sign up with a new email)
    2. Navigate to http://localhost:3000/campaigns
    3. Confirm: empty state message appears ("No campaigns yet." or similar)
    4. Confirm: "Create new campaign" button is still visible

    Test 3 — Unauthenticated visitor:
    1. Log out
    2. Navigate to http://localhost:3000/campaigns
    3. Confirm: redirected to /auth/login (middleware protection)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
TypeScript compiles cleanly: `npx tsc --noEmit`

The campaign list query filters by dmId — DMs only see their own campaigns.

The page is a Server Component — no client-side state or loading flicker.

Existing middleware already redirects unauthenticated visitors to /auth/login before the page renders — no additional auth guard needed in the page itself (though a null-safety fallback redirect is included).
</verification>

<success_criteria>
1. DM's home page (/campaigns) shows a card for each of their campaigns, displaying the campaign name — CAMP-04
2. DM can click a campaign card to navigate to that campaign's dashboard — CAMP-04
3. DM home page has a "Create new campaign" button that navigates to /campaigns/new — CAMP-05
4. A DM with no campaigns sees an empty state with only the "Create new campaign" button — CAMP-04
</success_criteria>

<output>
After completion, create `.planning/phases/10-multi-campaign-dashboard/10-01-SUMMARY.md` using the summary template.
</output>
