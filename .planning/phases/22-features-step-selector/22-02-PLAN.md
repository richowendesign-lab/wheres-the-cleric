---
phase: 22-features-step-selector
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified: []
autonomous: false
requirements:
  - FEAT-02

must_haves:
  truths:
    - "Human has confirmed clicking each step highlights it, shows its description, and swaps the illustration"
    - "Human has confirmed only one step is active at a time"
    - "Human has confirmed Phase 21 scroll animation still fires on FeaturesBlock"
  artifacts:
    - path: "src/components/landing/FeaturesBlock.tsx"
      provides: "Interactive step-selector — verified working in browser"
  key_links:
    - from: "step card click"
      to: "illustration image swap"
      via: "activeStep state → template literal src"
      pattern: "features-step-\\$\\{activeStep\\}\\.png"
---

<objective>
Start the dev server and verify the interactive step-selector works correctly in the browser. Confirms all three success criteria for FEAT-02 are met before the phase is marked complete.

Purpose: Human-verified acceptance before the phase closes.
Output: Phase 22 sign-off; SUMMARY written.
</objective>

<execution_context>
@/Users/richardowen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardowen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: step-selector interaction and animation in browser</name>
  <what-built>
    Interactive FeaturesBlock step-selector: clicking any of the four step cards highlights it, expands its description, and swaps the illustration image. Only one step is active at a time. Step 1 is active on page load. Phase 21 scroll animation preserved.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` (runs on http://localhost:3000)
    2. Visit http://localhost:3000 in a browser (as a logged-out visitor — use incognito if needed)
    3. Scroll down to the "Simple scheduling for your next game" section
    4. Confirm Step 1 card shows a dark purple badge, full opacity, and the description "Fill in basic details, set a planning window and share the link with players"
    5. Confirm the right column shows the features-step-1.png illustration
    6. Click Step 2 — confirm Step 2 card becomes highlighted (dark badge, full opacity, description appears), Step 1 becomes dim (light badge, opacity-60, no description)
    7. Confirm the illustration changes to features-step-2.png
    8. Click Step 3 — confirm same behaviour: Step 3 active, Step 2 dims
    9. Click Step 4 — confirm same behaviour: Step 4 active, Step 3 dims
    10. Click Step 1 again — confirm it becomes active again and Step 4 dims
    11. Scroll back up and then down to the section — confirm the fade + slide-up animation still fires on scroll entry (Phase 21 animation intact)
  </how-to-verify>
  <resume-signal>Type "approved" if all 11 checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All three FEAT-02 success criteria confirmed by human:
1. Clicking any step card highlights it and expands its description
2. Clicking a step swaps the illustration image
3. Only one step active at a time; switching deactivates the previous
</verification>

<success_criteria>
Human approves all verification steps. Phase 22 complete. FEAT-02 satisfied.
</success_criteria>

<output>
After approval, create `.planning/phases/22-features-step-selector/22-02-SUMMARY.md`
</output>
