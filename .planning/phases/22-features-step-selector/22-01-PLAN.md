---
phase: 22-features-step-selector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/FeaturesBlock.tsx
autonomous: true
requirements:
  - FEAT-02

must_haves:
  truths:
    - "Clicking step 2, 3, or 4 highlights that card (dark purple badge, full opacity) and shows its description"
    - "The illustration image swaps to match the active step"
    - "Only one step is active at a time — selecting a new step deactivates the previous one"
    - "Step 1 is active and its description is visible on page load"
    - "Phase 21 scroll animation (fade + slide-up on viewport entry) is preserved unchanged"
  artifacts:
    - path: "src/components/landing/FeaturesBlock.tsx"
      provides: "Interactive step-selector component"
      contains: "useState"
  key_links:
    - from: "card div onClick"
      to: "setActiveStep(step.id)"
      via: "onClick handler on each card"
      pattern: "onClick.*setActiveStep"
    - from: "activeStep state"
      to: "img src"
      via: "template literal"
      pattern: "features-step-\\$\\{activeStep\\}\\.png"
---

<objective>
Convert FeaturesBlock from a static hard-coded shell into an interactive step-selector. Add one useState hook, replace four hard-coded card divs with a data-driven steps.map(), and derive all visual differences (badge colour, card opacity, description visibility, illustration src) from the activeStep integer.

Purpose: Satisfies FEAT-02 — visitors can click any step to explore each scheduling stage.
Output: Updated src/components/landing/FeaturesBlock.tsx with full click interactivity.
</objective>

<execution_context>
@/Users/richardowen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardowen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/landing/FeaturesBlock.tsx
@src/hooks/useInView.ts
</context>

<interfaces>
<!-- Current FeaturesBlock.tsx — executor replaces this in place. Do NOT create a new file. -->
<!-- useInView hook signature (must be preserved): -->
```typescript
// src/hooks/useInView.ts
export function useInView(options?: { threshold?: number }): {
  ref: React.RefObject<Element>
  inView: boolean
}
```

<!-- Design tokens from globals.css (verified in research): -->
<!-- --dnd-card-bg, --dnd-border-card (#572182), --dnd-accent (#ba7df6), --dnd-text-muted (#a19aa8) -->

<!-- Step copy (verbatim — do not paraphrase): -->
<!-- Step 1 title: "Create and share your campaign" -->
<!-- Step 1 description: "Fill in basic details, set a planning window and share the link with players" -->
<!-- Step 2 title: "Players mark their availability" -->
<!-- Step 2 description: "Everyone sets their free days — you see it live on the calendar." -->
<!-- Step 3 title: "Add your unavailable dates" -->
<!-- Step 3 description: "Block dates when you cannot run a session in the Settings tab." -->
<!-- Step 4 title: "Pick the best day" -->
<!-- Step 4 description: "The ranked list shows which days work for everyone - copy it to your group chat." -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite FeaturesBlock.tsx with useState step-selector</name>
  <files>src/components/landing/FeaturesBlock.tsx</files>
  <action>
    Replace the current static hard-coded four-card structure with a data-driven interactive implementation. The complete rewrite must:

    1. Add `useState` to the React import: `import { useState } from 'react'`
    2. Keep the existing `useInView` import unchanged
    3. Define a `steps` const array ABOVE the component (not inside) with four objects, each `{ id: number, title: string, description: string }` using 1-indexed ids (1–4) and verbatim copy from research:
       - id:1 title:"Create and share your campaign" description:"Fill in basic details, set a planning window and share the link with players"
       - id:2 title:"Players mark their availability" description:"Everyone sets their free days — you see it live on the calendar."
       - id:3 title:"Add your unavailable dates" description:"Block dates when you cannot run a session in the Settings tab."
       - id:4 title:"Pick the best day" description:"The ranked list shows which days work for everyone - copy it to your group chat."
    4. Inside the component, add `const [activeStep, setActiveStep] = useState(1)` — initialised to 1 (NOT 0)
    5. Keep the existing `const { ref, inView } = useInView({ threshold: 0.1 })` unchanged
    6. Preserve the `<section>` element with `ref={ref as React.RefObject<HTMLElement>}` and all three transition Tailwind classes unchanged (the scroll animation from Phase 21 must not break)
    7. Keep the `<h2>` heading and `<p>` subtitle paragraph unchanged
    8. Replace the four hard-coded step card divs with `{steps.map((step) => { const isActive = activeStep === step.id; return (...) })}` inside the existing left-column div
    9. Each mapped card div must:
       - Have `key={step.id}`
       - Have `onClick={() => setActiveStep(step.id)}`
       - Apply `cursor-pointer` class
       - Apply `opacity-60` class ONLY when NOT active (do NOT add `pointer-events-none` — inactive cards must remain clickable)
       - Render a badge `<span>` with `bg-[#572182] text-white` when active, `bg-[#ba7df6] text-black` when inactive
       - Conditionally render the description `<p>` only when `isActive` is true: `{isActive && <p className="text-[var(--dnd-text-muted)] mt-1 text-sm">{step.description}</p>}`
    10. Replace the hard-coded `<img src="/features-step-1.png">` with `<img src={`/features-step-${activeStep}.png`} alt={`Step ${activeStep} illustration`} width={308} height={308} className="w-[308px] h-[308px] object-contain mx-auto" />`
    11. Do NOT switch from `<img>` to Next.js `<Image>` — plain `<img>` is correct here per Phase 20 research

    PITFALLS TO AVOID (from research):
    - Do not use `useState(0)` — use `useState(1)`. The image filenames and badge numbers are 1-indexed.
    - Do not add `pointer-events-none` to inactive cards.
    - Do not remove or alter the `useInView` hook usage or the `<section>` animation classes.
    - Do not mix hard-coded card divs with the map — all four cards must come from the map.
    - Use the verbatim step copy listed above — do not paraphrase.
  </action>
  <verify>
    <automated>cd /Users/richardowen/Desktop/wheres-the-cleric && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>FeaturesBlock.tsx compiles without TypeScript errors; contains `useState`, `steps.map`, `setActiveStep`, and the template literal image src; the four hard-coded step divs are gone; the useInView animation section wiring is intact</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes: `npx tsc --noEmit` returns no errors
- `FeaturesBlock.tsx` contains `useState` import
- `FeaturesBlock.tsx` contains `steps.map`
- `FeaturesBlock.tsx` contains `setActiveStep`
- `FeaturesBlock.tsx` contains `features-step-${activeStep}.png`
- `FeaturesBlock.tsx` does NOT contain any hard-coded `Step 1 — ACTIVE` comments or four separate static card divs
- `FeaturesBlock.tsx` still contains `useInView` and the three transition classes on `<section>`
</verification>

<success_criteria>
FeaturesBlock.tsx is a fully data-driven interactive component: one useState hook, one steps array, one map rendering four clickable cards with conditional badge colour / opacity / description, and a dynamic illustration image src. TypeScript compiles clean. Phase 21 animation is unbroken.
</success_criteria>

<output>
After completion, create `.planning/phases/22-features-step-selector/22-01-SUMMARY.md`
</output>
