---
phase: 21-scroll-animations
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified: []
autonomous: false
requirements:
  - ANIM-01
  - ANIM-02

must_haves:
  truths:
    - "Visitor sees sections animate in with fade + slide-up as they scroll down the page"
    - "Visitor with prefers-reduced-motion enabled sees sections appear immediately with no animation"
  artifacts: []
  key_links: []
---

<objective>
Human verification that scroll animations are working correctly in the browser, including reduced-motion behaviour.

Purpose: Code correctness (TypeScript + build) was verified in plan 01. This plan confirms the actual visual and motion behaviour in a real browser — the only way to catch threshold misconfiguration, flash-of-invisible-content, or motion that fires incorrectly.
Output: Confirmed working animations (or issues fed back for gap closure).
</objective>

<execution_context>
@/Users/richardowen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardowen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-scroll-animations/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server</name>
  <files></files>
  <action>
Start the Next.js development server so the human can verify animations in the browser.

Run: `npm run dev` and confirm the server is ready at http://localhost:3000. Output the URL to the user.
  </action>
  <verify>
    <automated>cd /Users/richardowen/Desktop/wheres-the-cleric && curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 | grep 200</automated>
  </verify>
  <done>Dev server is running and responding at http://localhost:3000</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify scroll animations in browser</name>
  <what-built>
Scroll animations on the landing page. Four sections (Hero, Features, Easy for Players, CTA) now use IntersectionObserver via useInView to fade and slide up on viewport entry. Reduced-motion users see instant appearance via Tailwind's motion-reduce:transition-none.
  </what-built>
  <action>Present the verification steps below to the user and wait for their response.</action>
  <how-to-verify>
**Step 1 — Normal animations:**
1. Open http://localhost:3000 in a browser (logged out, or use an incognito window)
2. Confirm the HeroSection fades and slides up immediately on page load (it should animate in within the first second)
3. Scroll slowly down the page
4. Confirm FeaturesBlock fades and slides up as it enters the viewport
5. Continue scrolling — confirm PlayersSection fades and slides up
6. Continue scrolling — confirm CtaSection fades and slides up
7. Scroll back up and then down again — confirm sections do NOT re-animate (they should stay visible, animation fires once only)

**Step 2 — Reduced motion:**
On Mac: System Settings -> Accessibility -> Display -> Reduce Motion (turn ON)
Then hard-refresh http://localhost:3000

8. Confirm the HeroSection is visible immediately with no fade/slide animation
9. Scroll down — confirm all sections appear instantly (visible, but no transition animation)
10. Turn Reduce Motion back OFF when done

**Expected results:**
- Normal: Smooth 700ms fade + subtle upward slide for each section as it enters view
- Reduced motion: All sections visible, no animation, just instant appearance
  </how-to-verify>
  <verify>
    <automated>MISSING — visual browser verification required; human must confirm</automated>
  </verify>
  <done>Human has confirmed: (1) all four sections animate on scroll entry with fade + slide-up, (2) HeroSection animates on page load, (3) animations do not re-trigger on scroll back, (4) Reduce Motion suppresses all animation</done>
  <resume-signal>Type "approved" if animations work correctly, or describe any issues seen</resume-signal>
</task>

</tasks>

<verification>
Human has confirmed:
1. All four sections animate on scroll entry (fade + slide up, 700ms duration)
2. HeroSection animates immediately on page load
3. Animations do not re-trigger on scroll back
4. With Reduce Motion enabled, sections appear without animation
</verification>

<success_criteria>
Phase 21 is complete when the human approves both:
- ANIM-01: Sections fade and slide up on viewport entry
- ANIM-02: Reduced motion users see no animation
</success_criteria>

<output>
After completion, create `.planning/phases/21-scroll-animations/21-02-SUMMARY.md` using the summary template.
</output>
