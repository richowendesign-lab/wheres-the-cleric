---
phase: 05-schema-migration
plan: "04"
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified: []
autonomous: false
requirements:
  - MIGR-01

must_haves:
  truths:
    - "prisma generate completes without errors"
    - "next build completes without errors"
    - "No TypeScript errors across the entire codebase"
    - "App runs locally and the campaign creation page loads without crashing"
  artifacts:
    - path: ".next"
      provides: "Production build output"
  key_links:
    - from: "prisma/schema.prisma"
      to: "src/generated/prisma"
      via: "prisma generate step in build"
      pattern: "prisma generate"
    - from: "src/generated/prisma"
      to: "src/app/**/*.tsx"
      via: "TypeScript compilation"
      pattern: "next build"
---

<objective>
Verify the full build passes after schema migration and TypeScript fixups across all plans in this phase.

Purpose: The definitive test that all v1.0 field references have been eliminated. `prisma generate && next build` is the exact build command in package.json — this must succeed before Phase 6 can begin.
Output: Passing build. Human confirmation that the dev server starts and the app is functional.
</objective>

<execution_context>
@/Users/richardowen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardowen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-schema-migration/05-02-SUMMARY.md
@.planning/phases/05-schema-migration/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full TypeScript check and production build</name>
  <files></files>
  <action>
    Run the full build from the project root (/Users/richardowen/Desktop/my-portfolio):

    Step 1 — TypeScript check only (fast feedback loop):
    ```
    npx tsc --noEmit
    ```
    If this returns any errors, READ the error output carefully and fix the offending files before proceeding to Step 2. Common remaining errors after this phase:
    - Any file still importing from a deleted component
    - Type assertions on Campaign or PlayerSlot that reference old field names
    - Any file using `slot.inviteToken` or `campaign.name` or `campaign.dmName`

    Fix errors by editing the specific files mentioned in the error output. Do not proceed to Step 2 until `npx tsc --noEmit` exits with code 0.

    Step 2 — Full production build (matches Vercel deployment build command):
    ```
    npm run build
    ```
    This runs `prisma generate && next build` per package.json scripts.build.

    If next build fails with TypeScript errors that `tsc --noEmit` did not catch, fix them and re-run `npm run build`.

    If next build fails for a non-TypeScript reason (e.g., missing env var), note the error in the SUMMARY but do not block — the TypeScript errors are the primary concern for this phase.
  </action>
  <verify>
    <automated>cd /Users/richardowen/Desktop/my-portfolio && npm run build 2>&1 | tail -20</automated>
    <manual>Check that the build output shows "Compiled successfully" or equivalent Next.js success message. No "Type error" lines should appear in the output.</manual>
  </verify>
  <done>npm run build exits with code 0. No TypeScript errors. Next.js build artifacts written to .next/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify app runs cleanly against new schema</name>
  <files></files>
  <action>
    Human verification step. Run the dev server and confirm all routes load correctly against the new v1.1 schema.
  </action>
  <verify>
    <automated>cd /Users/richardowen/Desktop/my-portfolio && npm run build 2>&1 | grep -E "error|Error|compiled" | tail -5</automated>
    <manual>
      1. Run the dev server: npm run dev in /Users/richardowen/Desktop/my-portfolio
      2. Visit http://localhost:3000 — home page should load
      3. Visit http://localhost:3000/campaigns/new — campaign creation form shows only two date pickers (no name/player fields)
      4. Submit the form with a valid date range — should redirect to /campaigns/[id] showing "Campaign Dashboard"
      5. Visit http://localhost:3000/invite/anything — should show stub "Coming soon" message (not a 500 error)
      6. Check terminal for any runtime errors
    </manual>
  </verify>
  <done>Human confirms: all three routes load without errors; campaign creation form shows date pickers only; new campaign can be created and detail page loads.</done>
  <what-built>
    Full schema migration complete:
    - New schema with joinToken/dmSecret on Campaign, no inviteToken on PlayerSlot
    - Database wiped (all v1.0 data cleared)
    - All TypeScript errors from removed fields fixed across: seed.ts, campaign.ts, campaigns/[id]/page.tsx, invite/[token]/page.tsx, CampaignForm.tsx
    - Production build passing
  </what-built>
  <how-to-verify>
    1. Run the dev server: `npm run dev` in /Users/richardowen/Desktop/my-portfolio
    2. Visit http://localhost:3000 — home page should load
    3. Visit http://localhost:3000/campaigns/new — campaign creation form should load (showing only two date pickers, no name/player fields)
    4. Submit the form with a valid date range — should redirect to /campaigns/[id] showing "Campaign Dashboard"
    5. Visit http://localhost:3000/invite/anything — should show the stub "Coming soon" message (not a 500 error)
    6. Check the terminal for any runtime errors
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Build passes:
```
cd /Users/richardowen/Desktop/my-portfolio && npm run build && echo "BUILD PASSED"
```
Must print BUILD PASSED.
</verification>

<success_criteria>
- `npm run build` exits code 0 (prisma generate + next build both succeed)
- No TypeScript errors anywhere in the codebase
- Dev server starts and all three routes load without 500 errors: /, /campaigns/new, /invite/[anything]
- Human confirms campaign creation form shows date pickers only (no name/player inputs)
</success_criteria>

<output>
After completion, create `.planning/phases/05-schema-migration/05-04-SUMMARY.md`
</output>
