---
phase: 14-dashboard-redesign
plan: "04"
type: execute
wave: 3
depends_on:
  - "14-03"
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - "DM can navigate between Availability and Settings tabs with no page reload"
    - "Calendar prev/next navigation works on a multi-month planning window"
    - "Best Days list shows unavailable player names"
    - "Settings tab holds Join Link, Planning Window, and Delete — none appear on Availability tab"
  artifacts:
    - path: "src/components/CampaignTabs.tsx"
      provides: "Verified tab component in production build"
      contains: "activeTab"
    - path: "src/components/DashboardCalendar.tsx"
      provides: "Verified navigation in production build"
      contains: "currentMonthIndex"
  key_links:
    - from: "Availability tab"
      to: "DashboardCalendar with navigation"
      via: "CampaignTabs renders DashboardCalendar when activeTab === 'availability'"
      pattern: "activeTab.*availability"
---

<objective>
Human verification of the complete Phase 14 dashboard redesign.

Claude has automated all implementation in Plans 01-03. This checkpoint pauses for the DM (user) to visually verify the complete redesigned campaign dashboard end to end.

Purpose: Confirm all five DASH requirements are met before marking Phase 14 complete.
Output: Verified redesigned dashboard.
</objective>

<execution_context>
@/Users/richardowen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardowen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build and confirm zero errors</name>
  <files></files>
  <action>
Run the Next.js production build to confirm all TypeScript and compilation errors are resolved before the human verification checkpoint:

```bash
cd /Users/richardowen/Desktop/wheres-the-cleric && npm run build 2>&1 | tail -20
```

If build fails, fix errors now. Common issues to check:
- Missing or wrong import in page.tsx (e.g. still importing removed components)
- CampaignTabs props not matching what page.tsx passes
- UpdatePlanningWindowForm called with old `campaign` prop anywhere in the codebase

Once build passes, start the dev server:
```bash
cd /Users/richardowen/Desktop/wheres-the-cleric && npm run dev
```
  </action>
  <verify>
    <automated>cd /Users/richardowen/Desktop/wheres-the-cleric && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build completes with zero errors. Dev server running on http://localhost:3000.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify full Phase 14 dashboard redesign</name>
  <action>Pause for human to verify the complete redesigned campaign dashboard against all DASH requirements.</action>
  <what-built>
Complete Phase 14 dashboard redesign:
- Two-tab layout (Availability / Settings) on the campaign detail page
- Availability tab: Awaiting Response → DashboardCalendar+BestDaysList → DmExceptionCalendar
- Settings tab: Join Link + Max Players form → Planning Window form → Danger Zone (delete)
- DashboardCalendar: prev/next navigation arrows for multi-month windows, 2-up on large screens
- BestDaysList: shows names of unavailable players (not free players)
  </what-built>
  <how-to-verify>
Navigate to http://localhost:3000/campaigns, open any campaign.

**DASH-05 — Tabs:**
1. Confirm two tabs appear at top: "Availability" and "Settings"
2. Confirm "Availability" is active by default (underlined with purple accent)
3. Confirm Join Link, Planning Window, and Delete are NOT visible on the Availability tab
4. Click "Settings" tab — confirm Join Link, Planning Window update form, and Delete button appear
5. Click "Availability" tab — confirm calendar and best days appear again (no page reload)

**DASH-01 — Calendar navigation:**
6. If the campaign has a multi-month planning window: confirm left and right arrows appear in the calendar card header with a "N / total" counter
7. Click the next arrow — confirm the calendar advances to the next month
8. Click the prev arrow — confirm it goes back
9. Confirm the prev arrow is disabled (greyed out) on the first month; next arrow is disabled on the last month
10. On a large screen (lg breakpoint): confirm two months appear side by side when navigating to a position where a next month exists

**DASH-02 — Muted days:**
11. Confirm days outside the planning window appear in muted grey (not clickable buttons)

**DASH-03 — Best Days list:**
12. Confirm "Best Days" section appears to the right of the calendar (or below on mobile)
13. Confirm dates are ranked with numbers (1, 2, 3...)

**DASH-04 — Unavailable player names:**
14. On a day where some players are unavailable: confirm their names appear in the Best Days list entry (e.g. "(Alice unavailable)")
15. On a day where all players are free: confirm no names appear in parentheses

If a planning window is not set, set one in the Settings tab first to fully test DASH-01 through DASH-04.
  </how-to-verify>
  <verify>Human approves all checks above.</verify>
  <done>User types "approved" after confirming all five DASH checks pass.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All five DASH requirements verified by human inspection:
- DASH-01: Calendar prev/next navigation working
- DASH-02: Muted days outside planning window confirmed
- DASH-03: BestDaysList visible alongside calendar
- DASH-04: Unavailable player names shown
- DASH-05: Settings de-emphasised in separate tab
</verification>

<success_criteria>
- Human approves all five DASH checks
- No visual regressions on existing functionality (DmExceptionCalendar, ShareModal, editable title/description still work)
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-dashboard-redesign/14-04-SUMMARY.md`
</output>
