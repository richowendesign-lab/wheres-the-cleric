---
phase: 20-static-page-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/logo.svg
  - public/features-step-1.png
  - public/features-step-2.png
  - public/features-step-3.png
  - public/features-step-4.png
  - src/components/LandingPage.tsx
autonomous: false
requirements:
  - NAV-01
  - HERO-01
  - FEAT-01
  - FEAT-03
  - PLAY-01
  - CTA-01
  - CTA-02

must_haves:
  truths:
    - "StickyNav renders with logo, Beta badge, and hidden Sign up / Log in buttons"
    - "HeroSection shows app icon, 'schedule your next D&D Session' heading, subtitle, and CTA buttons"
    - "FeaturesBlock shows all 4 steps with step 1 visually active (dark badge, description visible) and steps 2–4 inactive (purple badge, no description)"
    - "FeaturesBlock right column shows static <img src='/features-step-1.png' /> at 308×308px"
    - "PlayersSection shows 'Easy for players' heading and 3-card grid with verbatim copy"
    - "CtaSection shows 'Ready to plan your next adventure?' heading with Sign up / Log in buttons"
    - "Footer shows '© Copyright 2026 - Rich Owen Design' in muted text"
    - "All 5 feature illustration assets exist in /public"
    - "logo.svg exists in /public"
  artifacts:
    - path: "src/components/LandingPage.tsx"
      provides: "Client boundary component rendering all landing sections"
      exports: ["LandingPage"]
    - path: "public/logo.svg"
      provides: "Where's the Cleric? logo for sticky nav"
    - path: "public/features-step-1.png"
      provides: "Step 1 illustration (scroll/map with magnifying glass)"
    - path: "public/features-step-2.png"
      provides: "Step 2 illustration (quill/feather pen on parchment)"
    - path: "public/features-step-3.png"
      provides: "Step 3 illustration (sleeping wizard with spellbook)"
    - path: "public/features-step-4.png"
      provides: "Step 4 illustration (adventurer party)"
  key_links:
    - from: "src/components/LandingPage.tsx"
      to: "public/logo.svg"
      via: "<img src='/logo.svg'> in StickyNav"
      pattern: "src=\"/logo\\.svg\""
    - from: "src/components/LandingPage.tsx"
      to: "public/features-step-1.png"
      via: "<img src='/features-step-1.png'> in FeaturesBlock"
      pattern: "src=\"/features-step-1\\.png\""
---

<objective>
Save all static assets required by the landing page (logo SVG, 4 feature illustration PNGs), then build the complete `LandingPage` client component with all five sections: StickyNav, HeroSection, FeaturesBlock (static step 1 active), PlayersSection, CtaSection + Footer.

Purpose: Establish the full visual shell of the marketing landing page so Phase 20 Plan 02 can wire it into page.tsx in a single step.
Output: `/public/logo.svg`, `/public/features-step-{1-4}.png`, `src/components/LandingPage.tsx` (with inline or imported sub-section components)
</objective>

<execution_context>
@/Users/richardowen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardowen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-static-page-shell/CONTEXT.md

<!-- Key interfaces and tokens the executor needs. No codebase exploration required. -->
<interfaces>
From src/app/page.tsx (current):
```typescript
// Auth pattern to PRESERVE in page.tsx (Plan 02 — not this plan):
import { redirect } from 'next/navigation'
import { getSessionDM } from '@/lib/auth'
const dm = await getSessionDM()
if (dm) redirect('/campaigns')
```

Existing design tokens from globals.css:
```css
--dnd-accent:           #ba7df6;   /* primary purple */
--dnd-accent-hover:     #c994f8;   /* button hover */
--dnd-card-bg:          rgba(20, 3, 38, 0.6);
--dnd-border-card:      #572182;   /* active step badge bg */
--dnd-text-muted:       #a19aa8;   /* footer / secondary text */
```

Existing reusable class patterns:
```typescript
// Filled button (Sign up)
"px-6 py-3 rounded bg-[var(--dnd-accent)] text-black font-semibold hover:bg-[var(--dnd-accent-hover)] transition-colors"

// Outlined button (Log in)
"px-6 py-3 rounded border border-[var(--dnd-accent)] text-[var(--dnd-accent)] font-semibold hover:bg-[#ba7df6]/10 transition-colors"

// Beta badge
"text-xs font-semibold bg-[#ba7df6]/20 text-[var(--dnd-accent)] border border-[#ba7df6]/40 rounded px-2 py-0.5"

// Card
"bg-[var(--dnd-card-bg)] border border-[var(--dnd-border-card)] rounded-lg"

// Fantasy heading
"font-fantasy text-4xl text-white"
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Provide asset files</name>
  <what-to-do>
    Before the executor can build the landing page, the following files must exist in `/public`:

    1. `/public/logo.svg` — The "Where's the Cleric?" SVG logo from Figma
       - Figma design URL: https://www.figma.com/design/DkkUL0pVfYBOt7txFselZv/My-Portfolio-—-UI-Capture?node-id=90-2
       - Export the logo SVG from Figma and save it to `/public/logo.svg`

    2. `/public/features-step-1.png` — Scroll/map with magnifying glass (step 1 active state)
    3. `/public/features-step-2.png` — Quill/feather pen on parchment (step 2 active state)
    4. `/public/features-step-3.png` — Sleeping wizard with spellbook (step 3 active state)
    5. `/public/features-step-4.png` — Adventurer party (step 4 active state)

    These 4 PNG files were mentioned as "provided by the user in the conversation" in CONTEXT.md. Save them to `/public/features-step-1.png` through `/public/features-step-4.png`.

    If the actual screenshot files for the hero and players sections are available, also save:
    - `/public/hero-screenshot.png` (672×329px — DM dashboard preview)
    - `/public/players-screenshot.png` (672×329px — player availability view)

    If screenshots are NOT available yet, the executor will render a styled placeholder div instead (see Task 2).
  </what-to-do>
  <resume-signal>Type "assets ready" when all files are in /public, or "use placeholders" if screenshot images are not yet available (logo.svg and feature PNGs are still required).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Build LandingPage client component with all sections</name>
  <files>src/components/LandingPage.tsx</files>
  <action>
    Create `src/components/LandingPage.tsx` as a `'use client'` component. Use sub-components split into `src/components/landing/` directory since the full page will exceed 200 lines. Create the following files:

    **src/components/landing/StickyNav.tsx**
    - `<header>` with `className="sticky top-0 z-50 w-full px-8 py-4 flex items-center justify-between"` and transparent background (no bg class — Phase 24 adds scroll-triggered bg)
    - Left: `<img src="/logo.svg" alt="Where's the Cleric?" className="h-8 w-auto" />` + Beta badge: `<span className="text-xs font-semibold bg-[#ba7df6]/20 text-[var(--dnd-accent)] border border-[#ba7df6]/40 rounded px-2 py-0.5 ml-3">Beta</span>`
    - Right: Sign up + Log in buttons wrapped in `<div className="flex gap-3 opacity-0 pointer-events-none">` — HIDDEN by default (Phase 24 reveals on scroll). Use `<Link href="/auth/signup">` and `<Link href="/auth/login">` with existing button class patterns

    **src/components/landing/HeroSection.tsx**
    - Full-width centered section: `<section className="flex flex-col items-center justify-center text-center px-8 py-24 gap-6">`
    - App icon: `<Image src="/dnd-icon.png" alt="" width={82} height={82} />` (use next/image for this one asset per research)
    - Heading: `<h1 className="font-fantasy text-5xl text-white max-w-2xl">schedule your next D&D Session</h1>`
    - Subtitle: `<p className="text-[var(--dnd-text-muted)] text-lg max-w-lg">Beat the final boss - scheduling - and coordinate your group's availability without the back-and-forth.</p>`
    - CTA buttons: `<div className="flex gap-4">` with Sign up (filled, `/auth/signup`) and Log in (outlined, `/auth/login`)
    - Screenshot placeholder: If `/public/hero-screenshot.png` exists (user provided), render `<div className="mt-12 rounded-xl overflow-hidden shadow-2xl border border-[#ba7df6]/20 max-w-[672px] w-full mx-auto"><img src="/hero-screenshot.png" alt="DM dashboard preview" width={672} height={329} className="w-full h-auto" /></div>`. If NOT provided, render a styled placeholder div: `<div className="mt-12 rounded-xl border border-[#ba7df6]/20 bg-[var(--dnd-card-bg)] max-w-[672px] w-full mx-auto h-[329px] flex items-center justify-center"><span className="text-[var(--dnd-text-muted)] text-sm">DM dashboard preview — Phase 23</span></div>`

    **src/components/landing/FeaturesBlock.tsx**
    - `<section className="px-8 py-24 max-w-5xl mx-auto w-full">`
    - Section heading: `<h2 className="font-fantasy text-3xl text-white text-center mb-4">Simple scheduling for your next game</h2>`
    - Section subtitle: `<p className="text-[var(--dnd-text-muted)] text-center max-w-2xl mx-auto mb-12">No more manually creating polls. No more back and forth. Simply create a campaign, share the link, and let your players tell you when they're free, leaving you to focus on practicing your accents for that new NPC.</p>`
    - Two-column layout: `<div className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">`
      - Left column: 4 step cards
      - Right column: `<img src="/features-step-1.png" alt="Step 1 illustration" width={308} height={308} className="w-[308px] h-[308px] object-contain mx-auto" />`
    - **Step 1 (ACTIVE — hard-coded):** `<div className="flex items-start gap-4 bg-[var(--dnd-card-bg)] border border-[var(--dnd-border-card)] rounded-lg px-4 py-3">` with number badge `<span className="w-7 h-7 rounded-full bg-[#572182] text-white flex items-center justify-center text-sm font-bold shrink-0">1</span>`, heading "Create and share your campaign", AND description paragraph `<p className="text-[var(--dnd-text-muted)] mt-1 text-sm">Fill in basic details, set a planning window and share the link with players</p>`
    - **Steps 2–4 (INACTIVE — hard-coded):** Same card structure but `opacity-60` added to outer div, number badge uses `bg-[#ba7df6] text-black`, NO description paragraph. Copy: Step 2 "Players mark their availability", Step 3 "Add your unavailable dates", Step 4 "Pick the best day"

    DO NOT add useState for the active step. This is Phase 20 static only. Phase 22 adds click interactivity.

    **src/components/landing/PlayersSection.tsx**
    - `<section className="px-8 py-24 max-w-5xl mx-auto w-full">`
    - Heading: `<h2 className="font-fantasy text-3xl text-white text-center mb-12">Easy for players</h2>`
    - 3-card grid: `<div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-16">`
    - Each card: `<div className="bg-[var(--dnd-card-bg)] border border-[var(--dnd-border-card)] rounded-lg p-6">` with `<h3 className="font-semibold text-white mb-2">` and `<p className="text-[var(--dnd-text-muted)] text-sm">`
    - Card 1: "Your DM will share a link with you" / "Fill in your name and you're ready to go."
    - Card 2: "Input what days you are free to play" / "You'll be able to select any day in the planning window"
    - Card 3: "That's literally it - you don't even need to save" / "Your availability will show on your DM's dashboard"
    - Screenshot placeholder below cards (same pattern as HeroSection): if `/public/players-screenshot.png` exists render it; if not, render styled placeholder div `h-[329px]` with "Player availability view — Phase 23" label

    **src/components/landing/CtaSection.tsx**
    - `<section className="flex flex-col items-center justify-center text-center px-8 py-24 gap-6">`
    - App icon: `<Image src="/dnd-icon.png" alt="" width={82} height={82} />`
    - Heading: `<h2 className="font-fantasy text-4xl text-white">Ready to plan your next adventure?</h2>`
    - CTA buttons: same Sign up (filled) + Log in (outlined) as hero section

    **src/components/landing/Footer.tsx**
    - `<footer className="text-center py-8 text-[var(--dnd-text-muted)] text-sm">© Copyright 2026 - Rich Owen Design</footer>`

    **src/components/LandingPage.tsx**
    ```typescript
    'use client'
    import { StickyNav } from './landing/StickyNav'
    import { HeroSection } from './landing/HeroSection'
    import { FeaturesBlock } from './landing/FeaturesBlock'
    import { PlayersSection } from './landing/PlayersSection'
    import { CtaSection } from './landing/CtaSection'
    import { Footer } from './landing/Footer'

    export function LandingPage() {
      return (
        <>
          <StickyNav />
          <HeroSection />
          <FeaturesBlock />
          <PlayersSection />
          <CtaSection />
          <Footer />
        </>
      )
    }
    ```

    All sections sit transparently on the existing global radial gradient in globals.css — NO per-section background colors.

    Use `<Link>` from `next/link` for all CTA buttons. Use `<Image>` from `next/image` ONLY for `/public/dnd-icon.png`. Use plain `<img>` for logo.svg and all features-step-N.png files.
  </action>
  <verify>
    <automated>cd /Users/richardowen/Desktop/wheres-the-cleric && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `src/components/LandingPage.tsx` exports `LandingPage` function
    - `src/components/landing/` contains StickyNav, HeroSection, FeaturesBlock, PlayersSection, CtaSection, Footer
    - TypeScript compiles without errors on the new files
    - All verbatim copy matches CONTEXT.md exactly
    - FeaturesBlock step 1 active state is hard-coded (no useState)
    - Nav CTA buttons present in DOM but wrapped in `opacity-0 pointer-events-none`
  </done>
</task>

</tasks>

<verification>
After Task 2, verify:
- `ls src/components/landing/` shows all 6 section files
- `grep -n "use client" src/components/LandingPage.tsx` confirms client boundary
- `grep -n "useState" src/components/landing/FeaturesBlock.tsx` returns nothing (no state — static)
- `grep -n "opacity-0 pointer-events-none" src/components/landing/StickyNav.tsx` confirms nav buttons are hidden
- `ls public/logo.svg public/features-step-1.png public/features-step-2.png public/features-step-3.png public/features-step-4.png` all exist
</verification>

<success_criteria>
- All 5 landing section components exist and render without TypeScript errors
- All 5 static assets exist in /public
- LandingPage.tsx is a 'use client' component that composes all sections
- No useState used in FeaturesBlock (Phase 20 is static)
- Nav CTA buttons present but hidden (opacity-0 pointer-events-none)
- All copy is verbatim from CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/20-static-page-shell/20-01-SUMMARY.md`
</output>
