---
phase: 20-static-page-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/app/page.tsx
autonomous: false
requirements:
  - INT-01

must_haves:
  truths:
    - "Logged-out visitor visiting / sees the full marketing landing page"
    - "Logged-in DM visiting / is redirected to /campaigns — auth guard intact"
    - "page.tsx is still an async Server Component (no 'use client' directive)"
    - "HowItWorksButton is removed from page.tsx"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Server Component auth guard + LandingPage render"
      contains: "getSessionDM"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/LandingPage.tsx"
      via: "import and render <LandingPage />"
      pattern: "LandingPage"
    - from: "src/app/page.tsx"
      to: "lib/auth"
      via: "getSessionDM() + redirect('/campaigns')"
      pattern: "getSessionDM"
---

<objective>
Rewire `src/app/page.tsx` to replace the existing centered single-screen layout with the new LandingPage component, while preserving the Server Component auth redirect guard exactly.

Purpose: The landing page becomes live at `/` for logged-out visitors; logged-in DMs continue to be redirected to `/campaigns`.
Output: Updated `src/app/page.tsx` (auth guard + `<LandingPage />`) — all prior content including HowItWorksButton removed.
</objective>

<execution_context>
@/Users/richardowen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardowen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-static-page-shell/CONTEXT.md
@.planning/phases/20-static-page-shell/20-01-SUMMARY.md

<interfaces>
From src/components/LandingPage.tsx (created in Plan 01):
```typescript
// Named export — import with braces
export function LandingPage(): JSX.Element
```

Auth pattern to preserve verbatim:
```typescript
import { redirect } from 'next/navigation'
import { getSessionDM } from '@/lib/auth'

export default async function HomePage() {
  const dm = await getSessionDM()
  if (dm) redirect('/campaigns')
  return <LandingPage />
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire page.tsx — auth guard + LandingPage</name>
  <files>src/app/page.tsx</files>
  <action>
    Replace the entire content of `src/app/page.tsx` with the following:

    ```typescript
    import { redirect } from 'next/navigation'
    import { getSessionDM } from '@/lib/auth'
    import { LandingPage } from '@/components/LandingPage'

    export default async function HomePage() {
      const dm = await getSessionDM()
      if (dm) redirect('/campaigns')
      return <LandingPage />
    }
    ```

    Critical constraints:
    - DO NOT add `'use client'` to this file — it must remain an async Server Component
    - DO NOT carry over the `HowItWorksButton` import or render
    - DO NOT carry over the `<main>` centered layout, Image import for dnd-icon, or Link imports from the old page.tsx — all of that is now inside LandingPage component
    - The auth guard MUST be the first two lines of the function body — `getSessionDM()` then `redirect()` — exactly as shown above
    - Verify with `grep -n "'use client'" src/app/page.tsx` returns nothing
  </action>
  <verify>
    <automated>cd /Users/richardowen/Desktop/wheres-the-cleric && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `src/app/page.tsx` is 10 lines or fewer
    - No `'use client'` directive present
    - No `HowItWorksButton` import
    - `getSessionDM()` and `redirect('/campaigns')` are the first two lines of the default export function
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify landing page renders correctly at /</name>
  <what-built>
    Complete marketing landing page shell at `/` with sticky nav, hero, features section, easy for players section, CTA, and footer. Logged-in DMs are still redirected to /campaigns.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000 in a browser (logged out)
    2. Verify the page shows (scroll down to check each section):
       - Sticky nav at top: Where's the Cleric? logo, Beta badge (nav CTA buttons should be invisible — that's correct for Phase 20)
       - Hero: app icon, "schedule your next D&D Session" heading, subtitle, Sign up / Log in buttons
       - Features section: "Simple scheduling for your next game" heading, step 1 visually active (dark badge, description visible), steps 2–4 inactive (purple badge, no description), step 1 illustration image on the right
       - Easy for players: "Easy for players" heading, 3-card grid with correct text
       - CTA section: "Ready to plan your next adventure?" heading, app icon, Sign up / Log in buttons
       - Footer: "© Copyright 2026 - Rich Owen Design" in muted text
    3. Log in as a DM, navigate to http://localhost:3000 — confirm redirect to /campaigns (auth guard working)
    4. Log out again — confirm / shows the landing page, not the old centered layout
  </how-to-verify>
  <resume-signal>Type "approved" if the landing page looks correct and the auth redirect works. Describe any visual issues if not approved.</resume-signal>
</task>

</tasks>

<verification>
- `grep -n "'use client'" src/app/page.tsx` — returns nothing (must stay Server Component)
- `grep -n "HowItWorksButton" src/app/page.tsx` — returns nothing (removed)
- `grep -n "getSessionDM" src/app/page.tsx` — returns a match (auth guard present)
- `grep -n "redirect" src/app/page.tsx` — returns a match (redirect to /campaigns present)
- `npx tsc --noEmit` — exits 0, no TypeScript errors
</verification>

<success_criteria>
- Logged-out visitor sees full marketing landing page at /
- Logged-in DM visiting / is redirected to /campaigns
- page.tsx is an async Server Component with no 'use client' directive
- HowItWorksButton is fully removed from page.tsx
- All Phase 20 requirements satisfied: NAV-01, HERO-01, FEAT-01, FEAT-03, PLAY-01, CTA-01, CTA-02, INT-01
</success_criteria>

<output>
After completion, create `.planning/phases/20-static-page-shell/20-02-SUMMARY.md`
</output>
