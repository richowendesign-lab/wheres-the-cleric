---
phase: 03-availability
plan: 04
type: execute
wave: 3
depends_on: [03-03]
files_modified: []
autonomous: false
requirements: [AVAIL-04]

must_haves:
  truths:
    - "Player can see their own name and campaign name at the top of the availability form"
    - "Player can toggle weekly days on/off and select time preferences"
    - "Changes auto-save and the 'Availability saved' indicator appears and fades"
    - "Player can click calendar dates to mark as busy or free, with visual state changes"
    - "Refreshing the page shows all previously saved availability pre-populated"
    - "The form works end-to-end: toggle a day, see it save, refresh, see it still selected"
  artifacts:
    - path: "src/app/invite/[token]/page.tsx"
      provides: "Invite page with fully working AvailabilityForm"
    - path: "src/components/AvailabilityForm.tsx"
      provides: "Root form with auto-save"
    - path: "src/components/WeeklySchedule.tsx"
      provides: "Day toggle component"
    - path: "src/components/AvailabilityCalendar.tsx"
      provides: "Calendar grid component"
    - path: "src/lib/actions/availability.ts"
      provides: "Persistence Server Actions"
  key_links:
    - from: "Browser interaction"
      to: "Database"
      via: "Toggle day → debounced saveWeeklyPattern → Prisma transaction → dev.db"
      pattern: "end-to-end"
    - from: "Page load"
      to: "Pre-populated form"
      via: "Server Component fetches availabilityEntries → passes as initialEntries → initialises Set/Map state"
      pattern: "end-to-end"
---

<objective>
Human verification of the complete Phase 3 availability feature end-to-end.

Purpose: All auto-save interactions, visual states, and data persistence must be verified by a human in a running browser — this cannot be fully automated.
Output: Confirmed working availability form on the invite page.
</objective>

<execution_context>
@/Users/richardowen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardowen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-availability/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and get a test invite URL</name>
  <files></files>
  <action>
    Run the dev server and retrieve a valid invite token for testing:

    1. Start the dev server: `npm run dev`

    2. Query the database for a valid invite token:
       ```bash
       npx tsx -e "
       import { prisma } from './src/lib/prisma';
       const slots = await prisma.playerSlot.findMany({ include: { campaign: true } });
       slots.forEach(s => console.log(\`http://localhost:3000/invite/\${s.inviteToken}  [\${s.campaign.name} — \${s.name}]\`));
       await prisma.\$disconnect();
       "
       ```

    3. Print all invite URLs to the terminal so the user can click one.

    4. Also ensure the campaign has a planning window set (needed for the calendar):
       ```bash
       npx tsx -e "
       import { prisma } from './src/lib/prisma';
       const campaigns = await prisma.campaign.findMany();
       campaigns.forEach(c => console.log(\`\${c.name}: window \${c.planningWindowStart} to \${c.planningWindowEnd}\`));
       await prisma.\$disconnect();
       "
       ```
       If no planning window is set, note this to the user so they can set one from the campaign detail page before testing the calendar section.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 | grep -q "200" && echo "Dev server running"</automated>
  </verify>
  <done>Dev server is running and at least one valid invite URL is printed to the terminal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of full Phase 3 availability flow</name>
  <files></files>
  <action>
    Human verifies the complete availability feature end-to-end in a running browser.
    See how-to-verify steps below.
  </action>
  <what-built>
    Complete Phase 3 availability feature:
    - Weekly day/time toggle grid on the invite page
    - Custom calendar showing planning window with date override states
    - Auto-save on every interaction with "Availability saved" status indicator
    - Pre-populated form state on page load from existing database entries
  </what-built>
  <how-to-verify>
    Open one of the invite URLs printed in Task 1. Work through these checks:

    **Weekly schedule:**
    1. You should see 7 day buttons (Sun–Sat) in a row — all initially unselected (grey/muted)
    2. Click "Fri" — it should highlight in amber/gold
    3. Time options (Morning / Afternoon / Evening) should appear beneath or near the Friday button
    4. Click "Evening" — it should highlight. Wait ~1–2 seconds — "Saving..." should appear briefly, then "Availability saved"
    5. Click "Sat" — it should highlight. Click "Afternoon" — observe "Saving..." → "Availability saved"
    6. Click "Fri" again — the day should de-select and its time options disappear. Observe save indicator.

    **Persistence (AVAIL-04):**
    7. Refresh the page (Cmd+R / F5). The selections you made (Sat Evening, or whatever you saved last) should be pre-populated — NOT reset to empty.

    **Date exceptions (calendar — only if planning window is set):**
    8. Below the weekly section, you should see month grids covering the planning window
    9. Days matching your weekly pattern should show a subtle amber tint
    10. Click a date — it should turn red (busy override). A save indicator should appear.
    11. Click the same date again — it should turn green (free override)
    12. Click it a third time — it should revert to the pattern state (amber tint if your weekly pattern includes that day of week, plain otherwise)
    13. Refresh — the override should still be visible (persisted)

    **Error state (optional — skip if it would be disruptive):**
    14. If you can temporarily disable network (browser DevTools → Network → Offline), toggle a day — "Couldn't save — try again" should appear with a retry link

    **Visual style:**
    15. The form should match the D&D dark theme (dark background, amber accents, same font style as the rest of the invite page)
    16. The form should be readable and usable on a narrow viewport (mobile width ~375px)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
  <verify>
    <manual>Human approves all verification checks above (weekly toggles, auto-save, calendar overrides, persistence across refresh, D&D visual style)</manual>
  </verify>
  <done>User types "approved" — all Phase 3 AVAIL requirements verified working end-to-end in the browser.</done>
</task>

</tasks>

<verification>
Phase 3 is complete when the human verify checkpoint is approved with all checks passing:
- Weekly toggle UI works with time sub-options
- Auto-save fires and indicator displays correctly
- Data persists across page refresh (AVAIL-04)
- Calendar shows overrides and persists them
- Visual style matches D&D theme
</verification>

<success_criteria>
- All four AVAIL requirements demonstrably working:
  - AVAIL-01: Player selects which days of the week they are free
  - AVAIL-02: Player selects time-of-day preference per day
  - AVAIL-03: Player marks specific dates as free/busy overrides
  - AVAIL-04: Player returns to invite URL and sees their existing availability
- Human approves the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/03-availability/03-04-SUMMARY.md`
</output>
