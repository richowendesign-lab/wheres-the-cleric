---
phase: 04-dashboard
plan: "04"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "DM can see the full group availability grid on the campaign page"
    - "Dot colours correctly represent each player's status (green/red/grey)"
    - "All-free days visually stand out with a green cell background"
    - "Hovering a day shows a tooltip with player names and their status"
    - "Clicking a day opens a side panel; clicking outside or pressing Escape closes it"
    - "Missing players section lists players who have not submitted availability"
    - "Best days section shows up to 5 ranked days with player counts and names"
  artifacts:
    - path: "src/app/campaigns/[id]/page.tsx"
      provides: "Full dashboard wired and verified by DM"
  key_links: []
---

<objective>
Human verification of the complete Phase 4 dashboard — the DM visually confirms the group availability grid, dot colours, green highlights, hover tooltip, click panel, missing players list, and best-days ranking all work correctly.

Purpose: This is the quality gate for Phase 4. Automated TypeScript checks confirm structure; this checkpoint confirms the visual and interactive experience is correct.

Output: Verified, working DM dashboard.
</objective>

<execution_context>
@/Users/richardowen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardowen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify complete Phase 4 dashboard</name>
  <action>
Run automated pre-checks before presenting to the user:
1. `npx tsc --noEmit` — must pass with zero errors
2. `npm run build` — must complete successfully
If either fails, fix the errors before proceeding to human verification.
  </action>
  <what-built>
    Complete Phase 4 dashboard:
    - src/lib/availability.ts — availability aggregation utility (resolvePlayerStatusOnDate, computeDayStatuses, computeBestDays)
    - src/components/DashboardCalendar.tsx — multi-dot calendar grid with hover tooltip and click-to-panel
    - src/components/BestDaysList.tsx — ranked top-5 session days
    - src/app/campaigns/[id]/page.tsx — extended with all dashboard sections
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to http://localhost:3000/campaigns/[any-campaign-id]
    2. Scroll down past the existing Invite Links and Planning Window sections
    3. Confirm "Awaiting Response" section appears listing any players with no entries (or is hidden if all players have submitted)
    4. Confirm "Group Availability" section shows the calendar grid:
       - Each day cell has small coloured dots (one per player)
       - Green dots = free, red dots = busy, grey dots = no response
       - Days where all players are free have a green-tinted cell background
    5. Hover over a day cell — confirm a tooltip appears showing each player's name and Free/Busy/No response status
    6. Click a day cell — confirm a side panel slides in from the right showing full per-player breakdown
    7. Click outside the panel (on the backdrop) — confirm panel closes
    8. Open panel again, press Escape — confirm panel closes
    9. Confirm "Best Days" section below the calendar shows up to 5 ranked days with format like: "#1 — Sat 8 Mar — 4/4 players free (Alice, Bob, Sam, Kate)"
    10. Confirm the existing Invite Links and Planning Window sections still work correctly (no regressions)
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Human sign-off on all 10 verification steps above.
</verification>

<success_criteria>
- All 10 visual/interaction checks pass
- No regressions in existing campaign page functionality
- DM can identify the best day to schedule a session from the dashboard in under 10 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard/04-04-SUMMARY.md`
</output>
