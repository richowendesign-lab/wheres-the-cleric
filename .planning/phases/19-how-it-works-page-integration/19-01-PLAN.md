---
phase: 19-how-it-works-page-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HowItWorksButton.tsx
  - src/app/page.tsx
  - src/app/campaigns/page.tsx
  - src/app/join/[joinToken]/page.tsx
  - src/app/join/[joinToken]/availability/page.tsx
autonomous: false
requirements:
  - HOW-01
  - HOW-02

must_haves:
  truths:
    - "A 'How it works' trigger is visible on the logged-out home page (/) and opens the modal"
    - "A '?' icon button near the heading on /campaigns opens the modal without navigating away"
    - "A 'How it works' trigger appears on the player join page (/join/[joinToken]) and opens the modal"
    - "A 'How it works' trigger appears on the player availability page (/join/[joinToken]/availability) and opens the modal"
    - "None of the four pages gain a 'use client' directive as a result of this change"
  artifacts:
    - path: "src/components/HowItWorksButton.tsx"
      provides: "Client island accepting optional defaultRole prop, threading it to HowItWorksModal"
      exports: ["HowItWorksButton"]
    - path: "src/app/page.tsx"
      provides: "Home page with HowItWorksButton below the Log In / Sign Up buttons"
      contains: "HowItWorksButton"
    - path: "src/app/campaigns/page.tsx"
      provides: "Campaigns page with HowItWorksButton near the heading"
      contains: "HowItWorksButton"
    - path: "src/app/join/[joinToken]/page.tsx"
      provides: "Join page with HowItWorksButton below the join form, defaultRole='player'"
      contains: "HowItWorksButton"
    - path: "src/app/join/[joinToken]/availability/page.tsx"
      provides: "Availability page with HowItWorksButton below the form, defaultRole='player'"
      contains: "HowItWorksButton"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/HowItWorksButton.tsx"
      via: "import (Server Component importing Client island)"
      pattern: "import.*HowItWorksButton"
    - from: "src/app/campaigns/page.tsx"
      to: "src/components/HowItWorksButton.tsx"
      via: "import (Server Component importing Client island)"
      pattern: "import.*HowItWorksButton"
    - from: "src/app/join/\\[joinToken\\]/page.tsx"
      to: "src/components/HowItWorksButton.tsx"
      via: "import with defaultRole='player'"
      pattern: "defaultRole.*player"
    - from: "src/components/HowItWorksButton.tsx"
      to: "src/components/HowItWorksModal.tsx"
      via: "defaultRole prop threaded through"
      pattern: "defaultRole"
---

<objective>
Wire HowItWorksButton into all four pages (home, campaigns, join, availability) and extend it to accept a defaultRole prop so player-facing pages open the modal pre-selected on the player perspective.

Purpose: HOW-01 and HOW-02 — every user type can access the explainer from the page they land on; the DM campaigns page uses a small icon button in the heading per the requirement.
Output: Four pages each containing HowItWorksButton, one updated component, no new 'use client' pages.
</objective>

<execution_context>
@/Users/richardowen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardowen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/HowItWorksButton.tsx
@src/components/HowItWorksModal.tsx
@src/app/page.tsx
@src/app/campaigns/page.tsx
@src/app/join/[joinToken]/page.tsx
@src/app/join/[joinToken]/availability/page.tsx
</context>

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From src/components/HowItWorksModal.tsx:
```typescript
interface HowItWorksModalProps {
  onClose: () => void
  defaultRole?: 'dm' | 'player'
}
export function HowItWorksModal({ onClose, defaultRole }: HowItWorksModalProps)
```

HowItWorksModal already accepts defaultRole. HowItWorksButton currently does NOT — it is a zero-prop component. This plan adds the prop.

Current HowItWorksButton (src/components/HowItWorksButton.tsx):
```typescript
'use client'
import { useState } from 'react'
import { HowItWorksModal } from './HowItWorksModal'

export function HowItWorksButton() {   // <-- no props currently
  const [open, setOpen] = useState(false)
  return (
    <>
      <button
        type="button"
        onClick={() => setOpen(true)}
        aria-label="How it works"
        className="inline-flex items-center gap-1.5 text-sm text-[var(--dnd-text-muted)] hover:text-white transition-colors px-2 py-1 rounded border border-[var(--dnd-border-muted)] hover:border-[var(--dnd-accent)]"
      >
        How it works
        <span
          aria-hidden="true"
          className="w-4 h-4 rounded-full border border-current flex items-center justify-center text-[10px] font-bold leading-none shrink-0"
        >
          ?
        </span>
      </button>
      {open && <HowItWorksModal onClose={() => setOpen(false)} />}
    </>
  )
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add defaultRole prop to HowItWorksButton and wire into all four pages</name>
  <files>
    src/components/HowItWorksButton.tsx,
    src/app/page.tsx,
    src/app/campaigns/page.tsx,
    src/app/join/[joinToken]/page.tsx,
    src/app/join/[joinToken]/availability/page.tsx
  </files>
  <action>
**Step A — Update HowItWorksButton.tsx**

Add an optional `defaultRole?: 'dm' | 'player'` prop and thread it to HowItWorksModal. The button label and styling stay exactly as they are. No other changes:

```typescript
'use client'

import { useState } from 'react'
import { HowItWorksModal } from './HowItWorksModal'

interface HowItWorksButtonProps {
  defaultRole?: 'dm' | 'player'
}

export function HowItWorksButton({ defaultRole }: HowItWorksButtonProps = {}) {
  const [open, setOpen] = useState(false)
  return (
    <>
      <button
        type="button"
        onClick={() => setOpen(true)}
        aria-label="How it works"
        className="inline-flex items-center gap-1.5 text-sm text-[var(--dnd-text-muted)] hover:text-white transition-colors px-2 py-1 rounded border border-[var(--dnd-border-muted)] hover:border-[var(--dnd-accent)]"
      >
        How it works
        <span
          aria-hidden="true"
          className="w-4 h-4 rounded-full border border-current flex items-center justify-center text-[10px] font-bold leading-none shrink-0"
        >
          ?
        </span>
      </button>
      {open && <HowItWorksModal onClose={() => setOpen(false)} defaultRole={defaultRole} />}
    </>
  )
}
```

**Step B — src/app/page.tsx (logged-out home page)**

Add `import { HowItWorksButton } from '@/components/HowItWorksButton'` at the top. Place `<HowItWorksButton />` (no defaultRole — defaults to 'dm') below the `<div className="flex gap-4">` Log In / Sign Up button row. Add `mt-6` spacing above it:

```tsx
      </div>
      <div className="mt-6">
        <HowItWorksButton />
      </div>
```

Do NOT add 'use client' to page.tsx. Importing a Client Component from a Server Component is correct Next.js App Router pattern.

**Step C — src/app/campaigns/page.tsx (DM campaigns page)**

Add `import { HowItWorksButton } from '@/components/HowItWorksButton'` at the top.

HOW-02 requires a small `?` icon button near the page heading. Place `<HowItWorksButton />` inside the existing `<div className="flex items-center justify-between mb-8">` heading row, as a third sibling alongside the heading block and the log-out form. Position it between the heading `<div>` and the `<form>` for log out — this keeps the three items in natural reading order (title | how-it-works | log out). The button's existing styling (small, muted, bordered with `?` circle) satisfies the `?` icon requirement per HOW-02.

Do NOT add 'use client' to campaigns/page.tsx.

**Step D — src/app/join/[joinToken]/page.tsx (player join page)**

Add `import { HowItWorksButton } from '@/components/HowItWorksButton'` at the top.

Place `<HowItWorksButton defaultRole="player" />` below the `<JoinForm .../>` element, inside the same `<div className="w-full max-w-sm flex flex-col items-center">` container. Add `mt-4` wrapper for spacing. Only add it in the "New visitor — render name entry form" return branch (the final return), NOT in the "Campaign Full" branch — players on a full campaign do not need to learn the flow.

Do NOT add 'use client' to join/[joinToken]/page.tsx.

**Step E — src/app/join/[joinToken]/availability/page.tsx (player availability page)**

Add `import { HowItWorksButton } from '@/components/HowItWorksButton'` at the top.

Place `<HowItWorksButton defaultRole="player" />` below the `<AvailabilityForm .../>` element. Add `mt-6` wrapper for spacing.

Do NOT add 'use client' to join/[joinToken]/availability/page.tsx.
  </action>
  <verify>
    <automated>cd /Users/richardowen/Desktop/wheres-the-cleric && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    TypeScript compiles without errors. Grep confirms: (1) no 'use client' added to any of the four page files, (2) HowItWorksButton imported in all four pages, (3) defaultRole="player" present in join and availability pages.

    Quick grep check commands:
    - `grep -n "use client" src/app/page.tsx src/app/campaigns/page.tsx src/app/join/\[joinToken\]/page.tsx src/app/join/\[joinToken\]/availability/page.tsx` — must return NO matches
    - `grep -rn "HowItWorksButton" src/app/` — must show all four page files
    - `grep -rn 'defaultRole="player"' src/app/` — must show join and availability pages
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — all four pages</name>
  <action>Present verification instructions to the user. Run `npm run dev` if dev server is not already running. Pause for user confirmation before proceeding.</action>
  <what-built>HowItWorksButton wired into all four pages. Home and campaigns pages open the modal in DM mode by default; join and availability pages open it in player mode by default.</what-built>
  <how-to-verify>
Run `npm run dev` and verify each page:

1. **Home page** (`http://localhost:3000/`) — log out first if already authenticated
   - Confirm "How it works ?" button is visible below the Log In / Sign Up buttons
   - Click it — modal opens, "I'm the DM" tab is active by default
   - Press Escape — modal closes
   - Click backdrop — modal closes

2. **Campaigns page** (`http://localhost:3000/campaigns`) — log in as a DM
   - Confirm "How it works ?" button is visible near the "Your Campaigns" heading
   - Confirm it is NOT a full-width button — it should be compact/inline near the heading
   - Click it — modal opens, "I'm the DM" tab is active by default
   - Close modal — confirm you are still on the campaigns page (no navigation)

3. **Join page** — use a real or test join link (`http://localhost:3000/join/[any-valid-token]`)
   - Confirm "How it works ?" button is visible below the name entry form
   - Click it — modal opens, "I'm a player" tab is active by default

4. **Availability page** (`http://localhost:3000/join/[token]/availability` — after entering a name)
   - Confirm "How it works ?" button is visible below the availability form
   - Click it — modal opens, "I'm a player" tab is active by default

5. **No regressions** — confirm existing page functionality works:
   - Home page Log In and Sign Up links still work
   - Campaigns page log out still works
   - Campaigns page campaign cards still navigate correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
TypeScript passes with `npx tsc --noEmit`.

Grep checks before human verification:
- No page file gains 'use client'
- All four pages import HowItWorksButton
- Player pages pass defaultRole="player"
</verification>

<success_criteria>
1. HowItWorksButton accepts optional defaultRole prop and passes it to HowItWorksModal
2. Trigger visible and functional on /: below Log In/Sign Up, opens modal in DM mode
3. Trigger visible and functional on /campaigns: near heading, opens modal in DM mode, no navigation
4. Trigger visible and functional on /join/[joinToken]: below join form, opens modal in player mode
5. Trigger visible and functional on /join/[joinToken]/availability: below availability form, opens modal in player mode
6. Zero 'use client' directives added to any page file
</success_criteria>

<output>
After completion, create `.planning/phases/19-how-it-works-page-integration/19-01-SUMMARY.md`
</output>
